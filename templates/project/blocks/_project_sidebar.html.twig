<aside class="projects-sidebar">
    <div class="projects-sidebar__intro">
        <h1 class="projects-sidebar__title">Проекты</h1>
        <p class="projects-sidebar__lead">Рабочее пространство по проектам и моделям.</p>
    </div>

    {% set sidebarItems = page.sidebarItems|default([]) %}

    {% if sidebarItems %}
        <nav class="projects-sidebar__nav" aria-label="Список проектов">
            {% for project in sidebarItems %}
                <div class="projects-sidebar__row {% if project.isActive %}projects-sidebar__row--active{% endif %}">
                    <a
                        class="projects-sidebar__item {% if project.isActive %}projects-sidebar__item--active{% endif %}"
                        href="{{ project.shortId ? path('app_project_show', { shortId: project.shortId }) : '#' }}"
                        data-turbo-frame="project_content"
                        {% if not project.shortId %}aria-disabled="true"{% endif %}
                    >
                        <span class="projects-sidebar__name">{{ project.title|default('Без названия') }}</span>
                        <span class="projects-sidebar__meta">{{ project.code|default('NO CODE') }}</span>
                    </a>
                    <a
                        class="projects-sidebar__edit-btn"
                        href="{{ project.shortId ? path('app_project_edit', { shortId: project.shortId }) : '#' }}"
                        aria-label="Редактировать проект"
                        data-turbo-frame="project_content"
                        {% if not project.shortId %}aria-disabled="true"{% endif %}
                    >
                        ✎
                    </a>
                </div>
            {% endfor %}
        </nav>
    {% else %}
        <p class="projects-sidebar__empty">Проектов пока нет.</p>
    {% endif %}
</aside>
